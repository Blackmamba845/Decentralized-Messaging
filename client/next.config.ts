import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
   allowedDevOrigins: ['svc-258569a4-morphvm-8qoiipht.http.cloud.morph.so']
};

export default nextConfig;
